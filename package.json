{
  "name": "@shar-production/open-tools-hub",
  "version": "1.23.0",
  "description": "Bilingual static directory of verified SHAR Production public tools and datasets",
  "type": "module",
  "private": false,
  "license": "MIT",
  "author": "SHAR Production (https://sharprod.com/)",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check:build": "node scripts/build.mjs --check",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run build && npm test && npm run check:build",
    "verify:live": "node scripts/verify-live.mjs",
    "submit:indexnow": "node scripts/submit-indexnow.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
